<%= form_for @question do |q| %>
	<%= q.label :content %>
  <%= q.text_field :content %>
  <BR>
  <% @question.choices.each do |choice| %>
  	<%= q.fields_for :choices, choice do |c| %>
  	  Choice: <%= c.text_field :content %>
  	  <BR>
  	<% end %>	
  <% end %>
  <%= q.submit %>
<% end %>